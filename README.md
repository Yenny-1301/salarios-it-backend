crea el virtual environment para el proyecto
```
python -m venv venv
```

Activa el virtual environment
```
./venv/Scripts/activate
```

Instala Flask, SQLAlchemy, pyodbc, pandas y openpyxl
```
pip install flask flask_sqlalchemy pyodbc pandas openpyxl
```
